---
name: takelage-pvm-takelbase
version: 0.5.2

pvms:
  project:
    target_user: takelwerk
    target_repo: takelbase
    packer_template_dir: templates/takelbase/parallels

vboxes:
  project:
    target_user: takelwerk
    target_repo: takelbase
    packer_template_dir: templates/takelbase/virtualbox
